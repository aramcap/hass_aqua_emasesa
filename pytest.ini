[pytest]
testpaths = tests
# `tests/` no es un paquete: con esto los módulos de test importan
# `conftest` (helpers de fixtures) directamente, y `custom_components`
# se resuelve desde la raíz del repo (ver tests/conftest.py).
addopts = -q --import-mode=prepend
